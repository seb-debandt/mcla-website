---
const stats = [
  { value: '~4,000', label: 'New cases per year in the U.S.' },
  { value: '68', label: 'Median age at diagnosis' },
  { value: '~6%', label: 'Of all non-Hodgkin lymphomas' },
  { value: '3:1', label: 'Male-to-female ratio' },
];
---

<section class="py-20 md:py-24 bg-teal-primary relative overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-br from-teal-dark/50 to-transparent" aria-hidden="true"></div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
    <div class="text-center mb-14">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">MCL by the Numbers</h2>
      <p class="text-white/70 text-lg max-w-2xl mx-auto">
        Understanding the scope of mantle cell lymphoma.
      </p>
    </div>

    <div class="grid grid-cols-2 lg:grid-cols-4 gap-8 md:gap-12">
      {stats.map((stat) => (
        <div class="text-center">
          <div class="text-4xl md:text-5xl font-bold text-white mb-2 font-serif">{stat.value}</div>
          <div class="text-white/70 text-sm md:text-base">{stat.label}</div>
        </div>
      ))}
    </div>

    <p class="text-center text-white/40 text-xs mt-12">
      Statistics sourced from published medical literature including the American Cancer Society and the Leukemia &amp; Lymphoma Society.
    </p>
  </div>
</section>
